发现一个有意思的事情，所有的error监听，在devtool里面去throw一个error的时候都不能被捕获到

基于这个问题进行了查阅和本地测试 但是目前还未找到一个官方的地方去表明逻辑

不过经过这么多demo 的测试并结合别人的猜测
估计：chrome不希望在devtool中别的开发者导致的异常被实际网页端搜集到

所以在实际输入代码后整体被包裹了类似try catch的结构导致并没有被onerror捕获到

或者本身就处于不同的上下文(因为console就是类似一个扩展插件，那插件其实存在3个script 如果我没记错的话 可能本质就不在一块)

哎 目前先遗留解答 做个记录

正好在记录另一个事情 在top level是支持await的 但是在html中如果想要到top level 那必须在script上面增加type='module'


